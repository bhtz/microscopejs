var hubGenerator = module.exports = function () {
    var self = this;

   /*
    * Generate realtime hub controller with socketIO
    */
    self.generateHub = function (model) { 
    
    }

    return self;
}